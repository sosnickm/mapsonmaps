import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="mb-8">
            <h1 className="text-4xl font-bold text-center text-blue-600">Maps on Maps</h1>
        </header>
    );
};

export default Header;
